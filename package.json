{
  "name": "website-components",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "build:blog:all": "rg --files -g 'package.json' -g '!**/node_modules/**' | while read -r pkg; do if rg -q '\"build:blog\"' \"$pkg\"; then dir=$(dirname \"$pkg\"); echo \"==> $dir\"; bun run --cwd \"$dir\" build:blog; fi; done"
  },
  "devDependencies": {
    "@storybook/web-components-vite": "^10.1.11",
    "@types/bun": "latest",
    "storybook": "^10.1.11",
    "vite": "^7.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
